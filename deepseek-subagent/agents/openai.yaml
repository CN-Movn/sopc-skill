interface:
  display_name: "DeepSeek Subagent"
  short_description: "Current-process DeepSeek with durable handoffs"
  default_prompt: "Use $deepseek-subagent to run a V1 DeepSeek child with a verified project handoff log for every turn."
