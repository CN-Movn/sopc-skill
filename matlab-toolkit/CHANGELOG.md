# Changelog

## Initial import

- Add core `matlab-toolkit` skill entry.
- Add README describing scope and packaging boundaries.

The original uploaded package also contains additional reference documents, examples, and MATLAB template source files. These can be expanded in follow-up commits if needed.
