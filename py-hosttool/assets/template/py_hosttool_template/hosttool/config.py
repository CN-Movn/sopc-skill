"""Application identity and project-level defaults."""

APP_NAME = {{APP_NAME_LITERAL}}
APP_VERSION = {{APP_VERSION_LITERAL}}
# This is only the initial UI selection. Confirm it against the board's
# documented transport settings before implementing the project protocol.
DEFAULT_BAUDRATE = {{DEFAULT_BAUDRATE}}
DEFAULT_LAYOUT = "{{LAYOUT}}"
